---
title:    "Python: 「テキストファイルの読み込み」"
keywords: ["Python"]
---

{{< edit_this_page >}}

## なぜ
テキストファイルを読み込むことを学ぶのは、パイソンプログラミングの基本的なスキルの一つです。文字や数字を含むテキストファイルを正しく読み取ることができれば、プログラムをさまざまな形式のデータとやり取りさせることができます。これは、データ処理や分析を行う際に必要不可欠なスキルです。

## 作り方
テキストファイルを読み込むには、Pythonの組み込み関数である`open()`を使用します。`open()`には、ファイル名とファイルを開くモードを指定することで、ファイルを開くことができます。モードには、読み込みモード（`r`）、書き込みモード（`w`）、追記モード（`a`）などがあります。例えば、テキストファイル`sample.txt`を読み込む場合は、以下のように記述します。

```Python
f = open("sample.txt", "r")
```

ファイルを開いた後は、`read()`メソッドを使用してファイルを読み込みます。また、`readline()`メソッドを使用すれば、一行ずつ読み込むこともできます。例えば、次のように記述することで、テキストファイルの内容を表示することができます。

```Python
f = open("sample.txt", "r")
content = f.read()
print(content)
```

## 詳細を掘り下げる
テキストファイルを読み込む際には、ファイルがエンコーディングされていることにも注意が必要です。特に、多言語を含むテキストファイルを読み込む際は、ファイルのエンコーディングを指定することで文字化けを防ぐことができます。`open()`の引数として`encoding`オプションを指定することで、ファイルのエンコーディングを明示的に指定することができます。

また、テキストファイルを読み込む際には、ファイルの読み込み位置にも注意が必要です。`read()`や`readline()`を使用する際は、ファイルの内容がすべて読み込まれるまで読み進められます。そのため、ファイルの内容を読み込んだ後は、ファイルを閉じるために`close()`メソッドを使用することが重要です。

## 併せて見る
- [Python公式ドキュメント: `open()`](https://docs.python.org/ja/3/library/functions.html#open)
- [東京大学のPythonチュートリアル: ファイル](https://utokyo-prism.github.io/python-basic/file.html)
- [Qiita: テキストファイルの読み込み方法](https://qiita.com/lilycoco/items/a25d83fa838aa24ec559)