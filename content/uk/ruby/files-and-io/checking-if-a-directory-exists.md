---
title:                "Перевірка наявності директорії"
aliases:
- /uk/ruby/checking-if-a-directory-exists/
date:                  2024-02-03T19:08:24.117137-07:00
model:                 gpt-4-0125-preview
simple_title:         "Перевірка наявності директорії"
tag:                  "Files and I/O"
editURL:              "https://github.com/dogweather/forkful/blob/master/content/uk/ruby/checking-if-a-directory-exists.md"
changelog:
  - 2024-02-03, gpt-4-0125-preview, translated from English
---

{{< edit_this_page >}}

## Що і Чому?
Перевірка існування каталогу в Ruby дозволяє програмістам перевіряти наявність каталогу перед виконанням операцій, таких як читання файлів або створення нових каталогів. Це важливо для уникнення помилок при роботі з файлами та забезпечення надійності маніпуляцій з файловою системою.

## Як це зробити:
Стандартна бібліотека Ruby надає прямі методи для перевірки існування каталогу. Ось як це зробити чистим Ruby, без необхідності використання сторонніх бібліотек:

```ruby
require 'fileutils'

# Перевірка на існування каталогу
if Dir.exist?('/path/to/directory')
  puts 'Каталог існує.'
else
  puts 'Каталог не існує.'
end
```
Зразок виводу:
```
Каталог існує.
```
Або:
```
Каталог не існує.
```

Крім використання `Dir.exist?`, ви також можете скористатися методом `File.directory?`, який повертає `true`, якщо заданий шлях є каталогом:

```ruby
if File.directory?('/path/to/directory')
  puts 'Каталог існує.'
else
  puts 'Каталог не існує.'
end
```
Обидва `Dir.exist?` та `File.directory?` є частиною стандартної бібліотеки Ruby і не вимагають використання зовнішніх гемів, що робить їх зручними і ефективними варіантами для перевірки каталогів.
