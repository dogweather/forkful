---
title:                "Знаходження довжини рядка"
html_title:           "Swift: Знаходження довжини рядка"
simple_title:         "Знаходження довжини рядка"
programming_language: "Swift"
category:             "Swift"
tag:                  "Strings"
editURL:              "https://github.com/dogweather/forkful/blob/master/content/uk/swift/finding-the-length-of-a-string.md"
---

{{< edit_this_page >}}

## Чому

Впевнений, багато з вас хоч раз у житті зіштовхувалися з задачею знаходження довжини рядка або тексту у програмі. Це дуже поширена задача, адже знаходження довжини рядка потрібне для багатьох різних цілей, таких як валідація введення користувача або обробка даних з файлів.

## Як це зробити

Розглянемо простий приклад у Swift, як отримати довжину рядка. Зробимо це за допомогою функції `count`, яка повертає кількість символів у рядку. Вона виглядає наступним чином:

```Swift
let str = "Привіт, світ!"
print(str.count)
```

Результатом виконання цих двох рядків буде число `12`, що і є довжиною рядка.

А що, якщо в рядку є деякі спеціальні символи, такі як емодзі? У цьому випадку функція `count` може повернути не очікуване значення. Тому краще використовувати функцію `utf8.count`, яка оброблює символи Unicode. Наприклад:

```Swift
let emoji = "👋🏼"
print(emoji.utf8.count)
```

У цьому випадку, результатом буде `8`, оскільки кожен емодзі складається з двох символів Unicode.

## Розбір глибокий

Ви могли помітити, що функція `count` повертає не тільки кількість символів, але й пробіли. Це пов'язано з тим, що функція рахує кількість символів у рядку, включаючи пробіли і інші спеціальні символи. Також варто знати, що для закодованих символів Unicode функція `count` повертає кількість байт, а не символів.

Іноді потрібно враховувати не лише кількість символів, але й кількість слів в рядку. У цьому випадку можна використовувати властивість `split`, яка розділяє рядок на масив рядків за роздільниками, наприклад символом пробілу, і повертає кількість елементів масиву:

```Swift
let sentence = "Я люблю Swift"
print(sentence.split(separator: " ").count)
```

Це виведе `3`, оскільки у реченні три слова.

## Дивись також

- [Офіційна документація Swift](https://docs.swift.org/swift-book/LanguageGuide/StringsAndCharacters.html)
- [Пошук підрядка в рядку за допомогою Swift](https://codereview.stackexchange.com/questions/145241/substring-search-in-swift)