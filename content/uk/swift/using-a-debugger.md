---
title:                "Використання дебагера"
date:                  2024-01-26T04:11:08.551186-07:00
model:                 gpt-4-0125-preview
simple_title:         "Використання дебагера"
programming_language: "Swift"
category:             "Swift"
tag:                  "Testing and Debugging"
editURL:              "https://github.com/dogweather/forkful/blob/master/content/uk/swift/using-a-debugger.md"
---

{{< edit_this_page >}}

## Що і чому?
Використання дебагера означає застосування спеціалізованих інструментів для тестування та перевірки вашого коду під час виконання. Це велика справа, тому що це дозволяє вам побачити, що відбувається під капотом, знайти помилки та краще зрозуміти поведінку вашого коду.

## Як користуватися:
Щоб використовувати дебагер в Xcode (середовище розробки для Swift), ви можете встановити точки зупину, перевіряти змінні та спостерігати за виразами. Ось приклад:

```Swift
func findFactorial(of number: Int) -> Int {
    if number == 0 {
        return 1
    }
    return number * findFactorial(of: number - 1)
}

let result = findFactorial(of: 5)
print(result)
```

Встановіть точку зупину, клацнувши ліворуч до номеру рядка в Xcode, та запустіть програму. Коли виконання досягне точки зупину, Xcode призупинить виконання. Тепер ви можете:

1. Перевірити значення змінних.
2. Виконати перехід на наступний рядок або увійти всередину функції за допомогою контролів дебагера.
3. Додати вирази до "списку спостереження", щоб моніторити зміни конкретних змінних або констант.

Ось що ви можете побачити у зоні дебагу:

```
(lldb) po number
5
(lldb) po result
120
```

## Глибоке занурення:
Дебагери є частиною ландшафту програмування з 1940-х років, еволюціонувавши від простих систем точок зупину до складних, UI-орієнтованих досвідів. Крім вбудованого дебагера Xcode, існують інші опції, включаючи сторонні інструменти, як-от LLDB (Low Level Debugger), який використовується Xcode "під капотом". Деякі навіть відлагоджують за допомогою операторів `print()` (з любов'ю називають "дебагінг печерного людини"), але це менш ефективно для великих проектів або складних помилок. Коли ви користуєтеся дебагером, ви маніпулюєте контролем виконання, інтроспекцією в реальному часі та маніпуляцією даними. Глибоке розуміння цих принципів дуже допомагає в ефективному дебагінгу.

## Дивіться також:
- [Посібник з дебагінгу в Xcode від Apple](https://developer.apple.com/documentation/xcode/debugging-your-app)
- [Швидкий старт з LLDB](https://lldb.llvm.org/use/tutorial.html)
- [Навчальний посібник з дебагінгу Swift від Ray Wenderlich](https://www.raywenderlich.com/966538-arc-and-memory-management-in-swift)
