---
title:                "Работа с комплексными числами"
aliases:
- /ru/elixir/working-with-complex-numbers.md
date:                  2024-01-29T00:05:28.454532-07:00
model:                 gpt-4-0125-preview
simple_title:         "Работа с комплексными числами"

tag:                  "Numbers"
editURL:              "https://github.com/dogweather/forkful/blob/master/content/ru/elixir/working-with-complex-numbers.md"
changelog:
  - 2024-01-29, gpt-4-0125-preview, translated from English
---

{{< edit_this_page >}}

## Что и зачем?
Комплексные числа имеют действительную и мнимую части (например, `3 + 4i`). Они используются в инженерии, физике и некоторых вычислительных задачах. Программисты работают с ними для симуляций, обработки сигналов и эффективного решения определённых типов математических задач.

## Как это сделать:
В Elixir нет встроенной поддержки комплексных чисел, поэтому мы создаём свои собственные решения или используем библиотеку, например, `ComplexNum`. Вот быстрый пример с использованием библиотеки:

```elixir
# Предполагая, что у вас установлен ComplexNum
defmodule ComplexMath do
  import ComplexNum

  def add(a, b) do
    ComplexNum.add(a, b)
  end
end

# Создаем комплексные числа и складываем их
c1 = {3, 4}   # представляет 3 + 4i
c2 = {2, -3}  # представляет 2 - 3i
result = ComplexMath.add(c1, c2)
IO.puts "Результат: #{inspect(result)}"
```

Это выведет:
```
Результат: {5, 1}
```

Это означает, что сумма `3 + 4i` и `2 - 3i` равна `5 + 1i`.

## Погружение в тему
Комплексные числа появились в истории из-за того, что обычные числа не могли обрабатывать квадратные корни из отрицательных чисел. И только в 17-м веке к ним стали относиться серьезно благодаря таким математикам, как Рене Декарт и Джероламо Кардано.

В Elixir для комплексных чисел часто используются кортежи вида `{3, 4}` или посвящённая этому библиотека, чтобы не изобретать велосипед. Библиотеки обычно лучше — они обрабатывают сложные моменты, такие как умножение и деление, которые усложняются из-за мнимой единицы 'i' (кстати: `i` в квадрате равно `-1`).

## Смотрите также
Проверьте эти ресурсы:
- [Библиотека ComplexNum](https://hex.pm/packages/complex_num) для менеджера пакетов Elixir, Hex.
- [Школа Elixir](https://elixirschool.com/en/), для продвинутых тем и упражнений по Elixir.
- [Erlang -- модуль math](http://erlang.org/doc/man/math.html), который использует Elixir «под капотом», для других математических потребностей.
